# Formatting Rule (Mandatory)

## Trigger
After **any update/modification** to the codebase (including refactors, bug fixes, new features, config changes that affect code style).

## Required Action
Always run:

```bash
composer run format